// Fixed badge values requested for the presentation. They must never limit the
// notification lists or change persisted read/unread status.
export const NOTIFICATION_DISPLAY_COUNTS = { priority: 22, all: 30 } as const;
