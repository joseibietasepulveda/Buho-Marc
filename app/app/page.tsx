"use client";

import Link from "next/link";
import { FormEvent, useEffect, useMemo, useState } from "react";
import "./buho-app.css";

type View = "dashboard" | "brands" | "matches" | "cases" | "notifications" | "users" | "settings";
type BrandStatus = "Activa" | "Procesando" | "Pausada" | "Con error" | "Archivada";
type MatchLevel = "Alta" | "Media" | "Baja";
type MatchStatus = "Pendiente" | "En observación" | "Descartada" | "Convertida en caso";
type CaseStage = "Evaluación" | "Preparación" | "Presentado" | "Seguimiento";

type Brand = { id: string; name: string; owner: string; registration: string; classes: string; country: string; status: BrandStatus; matches: number; cases: number; updated: string };
type Match = { id: string; brandId: string; brand: string; found: string; applicant: string; application: string; score: number; level: MatchLevel; status: MatchStatus; date: string; deadline?: string; source: string; owner: string };
type LegalCase = { id: string; title: string; brand: string; client: string; stage: CaseStage; priority: "Alta" | "Media" | "Baja"; deadline: string; owner: string; sourceMatch?: string };
type DemoUser = { id: string; name: string; email: string; createdAt: string; initials: string };
type Notice = { id: string; title: string; brand: string; urgency: "Alta" | "Media"; status: "Pendiente" | "Gestionada"; date: string; subject: string; body: string };
type DemoSnapshot = { brands: Brand[]; matches: Match[]; cases: LegalCase[]; users: DemoUser[]; notices: Notice[] };

const NAV: { id: View; label: string }[] = [
  { id: "dashboard", label: "Inicio" }, { id: "brands", label: "Marcas" },
  { id: "matches", label: "Coincidencias" }, { id: "cases", label: "Casos" },
  { id: "notifications", label: "Notificaciones" }, { id: "users", label: "Usuarios" },
  { id: "settings", label: "Configuración" },
];

const initialBrands: Brand[] = [
  { id: "BM-018", name: "NOVA FOODS", owner: "Grupo Nova SpA", registration: "1428891", classes: "29, 30, 35", country: "Chile", status: "Activa", matches: 8, cases: 2, updated: "Hoy, 09:42" },
  { id: "BM-017", name: "TERRA SUR", owner: "Terra Sur Limitada", registration: "1413208", classes: "33, 35", country: "Chile", status: "Activa", matches: 5, cases: 1, updated: "Hoy, 08:16" },
  { id: "BM-016", name: "CASA NUBE", owner: "Inmobiliaria Casa Nube", registration: "1399480", classes: "36, 37", country: "Chile", status: "Activa", matches: 4, cases: 1, updated: "Ayer, 18:32" },
  { id: "BM-015", name: "PULSO", owner: "Pulso Salud SpA", registration: "1377012", classes: "05, 10, 44", country: "Chile", status: "Procesando", matches: 0, cases: 0, updated: "Hace 12 min" },
  { id: "BM-014", name: "LINARIA", owner: "Laboratorios Linaria", registration: "1351164", classes: "03, 05", country: "Chile", status: "Pausada", matches: 3, cases: 0, updated: "12 ago, 16:10" },
];

const initialMatches: Match[] = [
  { id: "CO-2481", brandId: "BM-018", brand: "NOVA FOODS", found: "NOVA FUDS", applicant: "Alimentos del Pacífico Ltda.", application: "1570234", score: 94, level: "Alta", status: "Pendiente", date: "19 ago 2026", deadline: "27 ago 2026", source: "Diario Oficial", owner: "Rosario Vial" },
  { id: "CO-2479", brandId: "BM-017", brand: "TERRA SUR", found: "TERRA DEL SUR", applicant: "Viñas del Valle SpA", application: "1570189", score: 87, level: "Alta", status: "Pendiente", date: "19 ago 2026", deadline: "29 ago 2026", source: "Diario Oficial", owner: "Matías Soto" },
  { id: "CO-2472", brandId: "BM-016", brand: "CASA NUBE", found: "CASANUBE", applicant: "Desarrollos Urbanos S.A.", application: "1569912", score: 73, level: "Media", status: "En observación", date: "18 ago 2026", source: "INAPI", owner: "Rosario Vial" },
  { id: "CO-2468", brandId: "BM-018", brand: "NOVA FOODS", found: "NOVO FOOD LAB", applicant: "Novo Lab Chile", application: "1569781", score: 66, level: "Media", status: "Pendiente", date: "18 ago 2026", source: "INAPI", owner: "Camila León" },
  { id: "CO-2451", brandId: "BM-014", brand: "LINARIA", found: "LINAR", applicant: "Cosmética Natural Chile", application: "1569114", score: 48, level: "Baja", status: "Descartada", date: "15 ago 2026", source: "Diario Oficial", owner: "Camila León" },
];

const initialCases: LegalCase[] = [
  { id: "BM-1042", title: "Oposición Nova Fuds", brand: "NOVA FOODS", client: "Grupo Nova SpA", stage: "Preparación", priority: "Alta", deadline: "20 ago", owner: "Rosario Vial", sourceMatch: "CO-2481" },
  { id: "BM-1038", title: "Revisión Terra del Sur", brand: "TERRA SUR", client: "Terra Sur Limitada", stage: "Evaluación", priority: "Alta", deadline: "22 ago", owner: "Matías Soto", sourceMatch: "CO-2479" },
  { id: "BM-1036", title: "Oposición CasaNube", brand: "CASA NUBE", client: "Inmobiliaria Casa Nube", stage: "Presentado", priority: "Media", deadline: "02 sep", owner: "Camila León", sourceMatch: "CO-2472" },
  { id: "BM-1027", title: "Seguimiento resolución Pulso", brand: "PULSO", client: "Pulso Salud SpA", stage: "Seguimiento", priority: "Baja", deadline: "11 sep", owner: "Rosario Vial" },
];

const initialUsers: DemoUser[] = [
  { id: "US-001", name: "Rosario Vial", email: "rosario@estudio.cl", createdAt: "04 mar 2026", initials: "RV" },
  { id: "US-002", name: "Matías Soto", email: "matias@estudio.cl", createdAt: "18 abr 2026", initials: "MS" },
  { id: "US-003", name: "Camila León", email: "camila@estudio.cl", createdAt: "02 jun 2026", initials: "CL" },
];

const initialNotices: Notice[] = [
  { id: "NO-114", title: "Coincidencia de alta similitud", brand: "NOVA FOODS", urgency: "Alta", status: "Pendiente", date: "Hoy, 09:42", subject: "Alerta de marca: posible similitud con NOVA FOODS", body: "Hola,\n\nDetectamos la publicación de la solicitud NOVA FUDS, que presenta una similitud alta con la marca NOVA FOODS. La publicación corresponde a la solicitud N° 1570234 y su plazo preliminar de revisión vence el 27 de agosto de 2026.\n\nSugerimos revisar los antecedentes para definir si corresponde presentar una oposición. Puedes ver la referencia oficial en la ficha adjunta.\n\nQuedo atento a tus comentarios.\n\nSaludos," },
  { id: "NO-112", title: "Plazo legal próximo", brand: "TERRA SUR", urgency: "Alta", status: "Pendiente", date: "Ayer, 16:20", subject: "Próximo plazo: TERRA SUR", body: "Hola,\n\nTe informamos que el plazo asociado a la revisión de TERRA DEL SUR se encuentra próximo. Recomendamos confirmar la estrategia antes del 22 de agosto.\n\nSaludos," },
  { id: "NO-108", title: "Borrador de revisión disponible", brand: "CASA NUBE", urgency: "Media", status: "Gestionada", date: "15 ago, 11:08", subject: "Revisión de coincidencia para CASA NUBE", body: "Hola,\n\nYa se encuentra disponible nuestra revisión inicial de la solicitud CASANUBE. La mantendremos en observación y te avisaremos ante cualquier cambio.\n\nSaludos," },
];

const stages: CaseStage[] = ["Evaluación", "Preparación", "Presentado", "Seguimiento"];
const viewTitles: Record<View, [string, string]> = {
  dashboard: ["Buenos días, Rosario.", "Resumen de tu cartera y acciones prioritarias"], brands: ["Marcas monitoreadas", "Administra la cartera activa de tu organización"],
  matches: ["Coincidencias", "Revisa y clasifica los hallazgos de tu cartera"], cases: ["Seguimiento de casos", "Gestiona cada causa sin perder el contexto original"],
  notifications: ["Centro de notificaciones", "Prepara comunicaciones claras para tus clientes"], users: ["Usuarios activos", "Personas con acceso a la organización"],
  settings: ["Configuración", "Preferencias de la experiencia de monitoreo"],
};

function useDemoStorage<T>(key: string, initialValue: T) {
  const [value, setValue] = useState<T>(initialValue); const [ready, setReady] = useState(false);
  useEffect(() => { const timer = window.setTimeout(() => { try { const saved = window.localStorage.getItem(key); if (saved) setValue(JSON.parse(saved) as T); } catch { /* seeded fallback */ } setReady(true); }, 0); return () => window.clearTimeout(timer); }, [key]);
  useEffect(() => { if (ready) window.localStorage.setItem(key, JSON.stringify(value)); }, [key, ready, value]);
  return [value, setValue] as const;
}

function Tone({ children, tone = "neutral" }: { children: React.ReactNode; tone?: string }) { return <span className={`buho-tone tone-${tone}`}>{children}</span>; }
function EmptyState({ title, copy }: { title: string; copy: string }) { return <div className="buho-empty"><span>00</span><strong>{title}</strong><p>{copy}</p></div>; }

export default function BuhoAppPage() {
  const [view, setView] = useState<View>("dashboard");
  const [brands, setBrands] = useDemoStorage("buho-demo-brands", initialBrands);
  const [matches, setMatches] = useDemoStorage("buho-demo-matches", initialMatches);
  const [cases, setCases] = useDemoStorage("buho-demo-cases", initialCases);
  const [users, setUsers] = useDemoStorage("buho-demo-users", initialUsers);
  const [notices, setNotices] = useDemoStorage("buho-demo-notices", initialNotices);
  const [brandFormOpen, setBrandFormOpen] = useState(false); const [caseFormOpen, setCaseFormOpen] = useState(false); const [userFormOpen, setUserFormOpen] = useState(false);
  const [selectedMatch, setSelectedMatch] = useState<string | null>(null); const [selectedCase, setSelectedCase] = useState<string | null>(null); const [selectedNotice, setSelectedNotice] = useState<string | null>(null);
  const [query, setQuery] = useState(""); const [levelFilter, setLevelFilter] = useState("Todas"); const [toast, setToast] = useState("");
  const [dataMode, setDataMode] = useState<"loading" | "database" | "local">("loading");

  useEffect(() => { const syncHash = () => { const hash = window.location.hash.replace("#", "") as View; if (NAV.some((item) => item.id === hash)) setView(hash); }; syncHash(); window.addEventListener("hashchange", syncHash); return () => window.removeEventListener("hashchange", syncHash); }, []);
  useEffect(() => { let active = true; fetch("/api/demo", { cache: "no-store" }).then(async (response) => { if (!active) return; if (!response.ok) { setDataMode("local"); return; } const payload = await response.json() as { data: DemoSnapshot }; applySnapshot(payload.data); setDataMode("database"); }).catch(() => active && setDataMode("local")); return () => { active = false; }; }, []); // eslint-disable-line react-hooks/exhaustive-deps
  useEffect(() => { if (!toast) return; const timer = window.setTimeout(() => setToast(""), 2400); return () => window.clearTimeout(timer); }, [toast]);
  const navigate = (next: View) => { setView(next); setQuery(""); window.history.pushState(null, "", `#${next}`); };
  const selectedMatchData = matches.find((match) => match.id === selectedMatch); const selectedCaseData = cases.find((item) => item.id === selectedCase); const selectedNoticeData = notices.find((item) => item.id === selectedNotice);
  const filteredMatches = useMemo(() => matches.filter((match) => (levelFilter === "Todas" || match.level === levelFilter) && `${match.brand} ${match.found} ${match.applicant} ${match.id}`.toLowerCase().includes(query.toLowerCase())).sort((a, b) => b.score - a.score), [levelFilter, matches, query]);

  function applySnapshot(snapshot: DemoSnapshot) { setBrands(snapshot.brands); setMatches(snapshot.matches); setCases(snapshot.cases); setUsers(snapshot.users); setNotices(snapshot.notices); }
  async function requestAction(input: Record<string, unknown>, sync = true) { if (dataMode !== "database") return false; try { const response = await fetch("/api/demo", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(input) }); const payload = await response.json() as { data?: DemoSnapshot; message?: string }; if (!response.ok) throw new Error(payload.message || "No fue posible guardar"); if (sync && payload.data) applySnapshot(payload.data); return true; } catch (error) { setToast(error instanceof Error ? error.message : "No fue posible guardar el cambio"); return false; } }
  async function addBrand(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const input = { action: "createBrand", name: String(data.get("name")), owner: String(data.get("owner")), registration: String(data.get("registration") || ""), classes: String(data.get("classes")), country: String(data.get("country")), description: String(data.get("description") || "") }; if (dataMode === "database") { if (!await requestAction(input)) return; } else { const next: Brand = { id: `BM-${String(brands.length + 19).padStart(3, "0")}`, name: input.name.toUpperCase(), owner: input.owner, registration: input.registration || "Pendiente", classes: input.classes, country: input.country, status: "Procesando", matches: 0, cases: 0, updated: "Ahora" }; setBrands([next, ...brands]); } setBrandFormOpen(false); setToast("Marca guardada; el motor queda pendiente de conexión"); navigate("brands"); }
  async function addCase(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const input = { action: "createCase", title: String(data.get("title")), brand: String(data.get("brand")), client: String(data.get("client")), priority: String(data.get("priority")), deadline: String(data.get("deadline")), owner: String(data.get("owner")), description: String(data.get("description") || "") }; if (dataMode === "database") { if (!await requestAction(input)) return; } else { const next: LegalCase = { id: `BM-${1043 + cases.length}`, title: input.title, brand: input.brand, client: input.client, stage: "Evaluación", priority: input.priority as LegalCase["priority"], deadline: input.deadline, owner: input.owner }; setCases([next, ...cases]); } setCaseFormOpen(false); setToast("Caso creado correctamente"); navigate("cases"); }
  async function addUser(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get("name")); const email = String(data.get("email")); if (dataMode === "database") { if (!await requestAction({ action: "createUser", name, email })) return; } else { const next: DemoUser = { id: `US-${String(users.length + 1).padStart(3, "0")}`, name, email, createdAt: "19 ago 2026", initials: name.split(" ").map((part) => part[0]).slice(0, 2).join("").toUpperCase() }; setUsers([...users, next]); } setUserFormOpen(false); setToast("Usuario agregado a la organización"); }
  async function reviewMatch(status: MatchStatus) { if (!selectedMatchData) return; if (dataMode === "database") { if (!await requestAction({ action: "reviewMatch", id: selectedMatchData.id, status })) return; } else { setMatches(matches.map((match) => match.id === selectedMatchData.id ? { ...match, status } : match)); if (status === "Convertida en caso" && !cases.some((item) => item.sourceMatch === selectedMatchData.id)) setCases([{ id: `BM-${1043 + cases.length}`, title: `Revisión ${selectedMatchData.found}`, brand: selectedMatchData.brand, client: brands.find((brand) => brand.id === selectedMatchData.brandId)?.owner ?? "Cliente", stage: "Evaluación", priority: selectedMatchData.level === "Alta" ? "Alta" : "Media", deadline: selectedMatchData.deadline?.replace(" 2026", "") ?? "Por definir", owner: selectedMatchData.owner, sourceMatch: selectedMatchData.id }, ...cases]); } setToast(status === "Convertida en caso" ? "Coincidencia convertida en caso" : `Coincidencia marcada: ${status}`); setSelectedMatch(null); }
  async function moveCase(item: LegalCase) { const index = stages.indexOf(item.stage); const nextStage = stages[Math.min(index + 1, stages.length - 1)]; if (dataMode === "database") { if (!await requestAction({ action: "moveCase", id: item.id, stage: nextStage })) return; } else setCases(cases.map((current) => current.id === item.id ? { ...current, stage: nextStage } : current)); setToast(`Caso movido a ${nextStage}`); }
  function updateNotice(id: string, patch: Partial<Notice>) { setNotices(notices.map((item) => item.id === id ? { ...item, ...patch } : item)); if (dataMode === "database") void requestAction({ action: "updateNotice", id, ...patch }, false); }
  async function resetData() { if (dataMode === "database") await requestAction({ action: "reset" }); else { setBrands(initialBrands); setMatches(initialMatches); setCases(initialCases); setUsers(initialUsers); setNotices(initialNotices); } setToast("Datos de demostración restaurados"); }
  async function copyText(text: string, label: string) { try { await navigator.clipboard.writeText(text); setToast(`${label} copiado`); } catch { setToast("Selecciona el texto y cópialo manualmente"); } }

  const mainAction = view === "cases" ? () => setCaseFormOpen(true) : view === "users" ? () => setUserFormOpen(true) : () => setBrandFormOpen(true);
  const mainActionLabel = view === "cases" ? "Agregar caso" : view === "users" ? "Agregar usuario" : "Agregar marca";

  return <main className="buho-app">
    <aside className="buho-sidebar"><Link className="buho-wordmark" href="/" aria-label="Buho Marc, volver al sitio">BUHO MARC</Link><span className={`buho-data-mode is-${dataMode}`}><i />{dataMode === "database" ? "DATOS EN RAILWAY" : dataMode === "loading" ? "CONECTANDO" : "MODO LOCAL"}</span><nav aria-label="Navegación de la aplicación">{NAV.map((item, index) => <button className={view === item.id ? "is-active" : ""} key={item.id} onClick={() => navigate(item.id)} type="button"><span>0{index + 1}</span>{item.label}{item.id === "notifications" && notices.filter((notice) => notice.status === "Pendiente").length > 0 && <b>{notices.filter((notice) => notice.status === "Pendiente").length}</b>}</button>)}</nav><div className="buho-plan"><span>PLAN ESTUDIO</span><strong>{brands.length} de 25 marcas</strong><i><b style={{ width: `${Math.min(100, brands.length / 25 * 100)}%` }} /></i><small>{Math.max(0, 25 - brands.length)} espacios disponibles</small><Link href="/#contacto">Ver pricing para incluir más</Link></div><button className="buho-profile" type="button"><span>RV</span><div><strong>Rosario Vial</strong><small>Estudio Vial IP</small></div></button></aside>
    <section className="buho-workspace"><header className="buho-topbar"><div><span className="buho-overline">MIÉRCOLES, 19 AGO 2026</span><h1>{viewTitles[view][0]}</h1><p>{viewTitles[view][1]}</p></div><div className="buho-actions"><label className="buho-search"><span>BUSCAR</span><input aria-label="Buscar en la vista actual" onChange={(event) => setQuery(event.target.value)} placeholder="Marca, caso o cliente" value={query} /></label><button className="buho-primary" onClick={mainAction} type="button">{mainActionLabel}<span>+</span></button></div></header>
      {view === "dashboard" && <Dashboard brands={brands} cases={cases} matches={matches} onMatch={setSelectedMatch} onNavigate={navigate} />}
      {view === "brands" && <BrandsView brands={brands} query={query} onAdd={() => setBrandFormOpen(true)} onOpenMatches={(name) => { navigate("matches"); window.setTimeout(() => setQuery(name), 0); }} />}
      {view === "matches" && <MatchesView matches={filteredMatches} level={levelFilter} onLevel={setLevelFilter} onSelect={setSelectedMatch} />}
      {view === "cases" && <CasesView cases={cases.filter((item) => `${item.title} ${item.brand} ${item.client}`.toLowerCase().includes(query.toLowerCase()))} onAdd={() => setCaseFormOpen(true)} onMove={moveCase} onSelect={setSelectedCase} />}
      {view === "notifications" && <NotificationsView notices={notices.filter((item) => `${item.title} ${item.brand}`.toLowerCase().includes(query.toLowerCase()))} onSelect={setSelectedNotice} />}
      {view === "users" && <UsersView users={users.filter((user) => `${user.name} ${user.email}`.toLowerCase().includes(query.toLowerCase()))} onAdd={() => setUserFormOpen(true)} />}
      {view === "settings" && <SettingsView dataMode={dataMode} onReset={() => void resetData()} onToast={setToast} />}
    </section>
    {brandFormOpen && <BrandForm onClose={() => setBrandFormOpen(false)} onSubmit={addBrand} brandsCount={brands.length} />}{caseFormOpen && <CaseForm brands={brands} users={users} onClose={() => setCaseFormOpen(false)} onSubmit={addCase} />}{userFormOpen && <UserForm onClose={() => setUserFormOpen(false)} onSubmit={addUser} />}
    {selectedMatchData && <MatchDrawer match={selectedMatchData} onClose={() => setSelectedMatch(null)} onReview={(status) => void reviewMatch(status)} />}{selectedCaseData && <CaseDrawer item={selectedCaseData} onClose={() => setSelectedCase(null)} onMove={() => void moveCase(selectedCaseData)} />}{selectedNoticeData && <NoticeDrawer notice={selectedNoticeData} onChange={(patch) => updateNotice(selectedNoticeData.id, patch)} onClose={() => setSelectedNotice(null)} onCopy={copyText} />}
    {toast && <div className="buho-toast" role="status"><span />{toast}</div>}
  </main>;
}

function Dashboard({ brands, matches, cases, onMatch, onNavigate }: { brands: Brand[]; matches: Match[]; cases: LegalCase[]; onMatch: (id: string) => void; onNavigate: (view: View) => void }) {
  const pending = matches.filter((item) => item.status === "Pendiente").sort((a, b) => b.score - a.score);
  return <><button className="buho-attention" onClick={() => pending[0] && onMatch(pending[0].id)} type="button"><div><span className="buho-live" />REQUIERE TU ATENCIÓN</div><strong>{pending.filter((item) => item.level === "Alta").length} coincidencias de alta similitud esperan revisión.</strong><span>Revisar ahora <b>→</b></span></button><div className="buho-metrics">
    <button onClick={() => onNavigate("brands")} type="button"><span>MARCAS ACTIVAS</span><strong>{brands.filter((item) => item.status === "Activa").length}</strong><small>{brands.length} de 25 espacios utilizados</small></button><button onClick={() => onNavigate("matches")} type="button"><span>NUEVAS COINCIDENCIAS</span><strong>{pending.length}</strong><small>{pending.filter((item) => item.level === "Alta").length} de alta similitud</small></button><button onClick={() => onNavigate("cases")} type="button"><span>CASOS ACTIVOS</span><strong>{String(cases.length).padStart(2, "0")}</strong><small>3 con plazo cercano</small></button><button type="button"><span>TIEMPO DE REVISIÓN</span><strong>1,8h</strong><small>12% mejor este mes</small></button></div>
    <div className="buho-dashboard-grid"><section className="buho-panel buho-review-panel"><header><div><span className="buho-overline">BANDEJA DE REVISIÓN</span><h2>Coincidencias nuevas</h2></div><button onClick={() => onNavigate("matches")} type="button">Ver todas</button></header>{pending.slice(0, 4).map((match) => <button className="buho-match-row" key={match.id} onClick={() => onMatch(match.id)} type="button"><Tone tone={match.level.toLowerCase()}>{match.level}</Tone><span><small>MARCA MONITOREADA</small><b>{match.brand}</b></span><span><small>SIGNO ENCONTRADO</small><b>{match.found}</b></span><strong>{match.score}%</strong><i>→</i></button>)}</section><aside className="buho-panel buho-deadlines"><header><div><span className="buho-overline">PRÓXIMOS 30 DÍAS</span><h2>Plazos</h2></div></header>{cases.slice(0, 4).map((item) => <button key={item.id} onClick={() => onNavigate("cases")} type="button"><time>{item.deadline.split(" ")[0]}<small>{item.deadline.split(" ")[1]?.toUpperCase()}</small></time><div><strong>{item.title}</strong><span>{item.brand} · {item.id}</span></div></button>)}</aside></div>
    <section className="buho-calendar"><header><div><span className="buho-overline">AGENDA LEGAL</span><h2>Agosto 2026</h2></div><div className="buho-segment"><button className="is-active" type="button">Mes</button><button type="button">Agenda</button></div></header><div className="buho-week"><span>LUN 17</span><span>MAR 18</span><span className="is-today">MIÉ 19</span><span>JUE 20</span><span>VIE 21</span><span>SÁB 22</span><span>DOM 23</span></div><div className="buho-calendar-events"><article style={{ gridColumn: 4 }}><span>20 AGO</span><strong>Respuesta a observación</strong><small>NOVA FOODS · Rosario</small></article><article style={{ gridColumn: 6 }}><span>22 AGO</span><strong>Presentar oposición</strong><small>TERRA SUR · Matías</small></article></div></section></>;
}

function BrandsView({ brands, query, onAdd, onOpenMatches }: { brands: Brand[]; query: string; onAdd: () => void; onOpenMatches: (name: string) => void }) {
  const filtered = brands.filter((item) => `${item.name} ${item.owner} ${item.registration}`.toLowerCase().includes(query.toLowerCase()));
  return <><section className="buho-quota"><div><span className="buho-overline">USO DEL PLAN</span><strong>{brands.length} de 25 marcas</strong><small>{25 - brands.length} espacios disponibles</small></div><i><b style={{ width: `${brands.length / 25 * 100}%` }} /></i><Link href="/#contacto">Ver pricing para incluir más marcas <span>↗</span></Link></section><section className="buho-table-panel"><header><div className="buho-tabs"><button className="is-active" type="button">Todas <b>{brands.length}</b></button><button type="button">Activas <b>{brands.filter((item) => item.status === "Activa").length}</b></button><button type="button">Procesando <b>{brands.filter((item) => item.status === "Procesando").length}</b></button></div><button className="buho-secondary" onClick={onAdd} type="button">Nueva marca</button></header>{filtered.length ? <div className="buho-table-wrap"><table><thead><tr><th>Marca</th><th>Titular</th><th>Clases</th><th>Estado</th><th>Coincidencias</th><th>Actualización</th><th /></tr></thead><tbody>{filtered.map((brand) => <tr key={brand.id}><td><span className="buho-monogram">{brand.name.slice(0, 2)}</span><div><strong>{brand.name}</strong><small>{brand.id} · Reg. {brand.registration}</small></div></td><td>{brand.owner}</td><td>{brand.classes}</td><td><Tone tone={brand.status === "Activa" ? "active" : brand.status === "Procesando" ? "processing" : "neutral"}>{brand.status}</Tone></td><td><button className="buho-number-link" onClick={() => onOpenMatches(brand.name)} type="button">{brand.matches}</button></td><td>{brand.updated}</td><td><button className="buho-more" type="button">···</button></td></tr>)}</tbody></table></div> : <EmptyState title="No encontramos marcas" copy="Prueba con otro término o agrega una nueva marca a la demo." />}</section></>;
}

function MatchesView({ matches, level, onLevel, onSelect }: { matches: Match[]; level: string; onLevel: (level: string) => void; onSelect: (id: string) => void }) {
  return <section className="buho-table-panel"><header><div className="buho-filter-group"><span>NIVEL</span>{["Todas", "Alta", "Media", "Baja"].map((item) => <button className={level === item ? "is-active" : ""} key={item} onClick={() => onLevel(item)} type="button">{item}</button>)}</div><div className="buho-table-actions"><button type="button">Vista guardada</button><button type="button">Exportar</button></div></header>{matches.length ? <div className="buho-table-wrap"><table><thead><tr><th>Nivel</th><th>Comparación</th><th>Puntaje</th><th>Solicitante</th><th>Estado</th><th>Plazo</th><th /></tr></thead><tbody>{matches.map((match) => <tr className="is-clickable" key={match.id} onClick={() => onSelect(match.id)}><td><Tone tone={match.level.toLowerCase()}>{match.level}</Tone></td><td><div className="buho-comparison"><span>{match.brand}</span><i>↔</i><strong>{match.found}</strong><small>{match.id} · Solicitud {match.application}</small></div></td><td><strong className="buho-score">{match.score}%</strong></td><td>{match.applicant}</td><td><Tone tone={match.status === "Pendiente" ? "processing" : match.status === "Descartada" ? "neutral" : "active"}>{match.status}</Tone></td><td>{match.deadline ?? "Sin plazo"}</td><td><button className="buho-row-arrow" type="button">→</button></td></tr>)}</tbody></table></div> : <EmptyState title="Sin coincidencias en esta vista" copy="Cambia el nivel o limpia la búsqueda para volver a ver resultados." />}</section>;
}

function CasesView({ cases, onAdd, onMove, onSelect }: { cases: LegalCase[]; onAdd: () => void; onMove: (item: LegalCase) => void; onSelect: (id: string) => void }) {
  return <><div className="buho-board-toolbar"><div><button className="is-active" type="button">Tablero</button><button type="button">Lista</button></div><div><button type="button">Responsable</button><button type="button">Prioridad</button><button className="buho-secondary" onClick={onAdd} type="button">Nuevo caso</button></div></div><section className="buho-kanban">{stages.map((stage) => { const items = cases.filter((item) => item.stage === stage); return <div className="buho-kanban-column" key={stage}><header><span>{stage}</span><b>{items.length}</b></header><div>{items.map((item) => <article key={item.id}><button className="buho-case-main" onClick={() => onSelect(item.id)} type="button"><div><Tone tone={item.priority.toLowerCase()}>{item.priority}</Tone><small>{item.id}</small></div><h3>{item.title}</h3><p>{item.brand} · {item.client}</p><footer><span><i />{item.deadline}</span><span>{item.owner.split(" ")[0]}</span></footer></button>{stage !== "Seguimiento" && <button className="buho-move" onClick={() => onMove(item)} type="button">Mover a {stages[stages.indexOf(stage) + 1]} <span>→</span></button>}</article>)}</div></div>; })}</section></>;
}

function NotificationsView({ notices, onSelect }: { notices: Notice[]; onSelect: (id: string) => void }) {
  return <section className="buho-notice-layout"><div className="buho-notice-list"><header><div className="buho-tabs"><button className="is-active" type="button">Todas <b>{notices.length}</b></button><button type="button">Pendientes</button><button type="button">Gestionadas</button></div></header>{notices.map((notice) => <button key={notice.id} onClick={() => onSelect(notice.id)} type="button"><span className={`buho-notice-dot ${notice.status === "Pendiente" ? "is-new" : ""}`} /><div><span className="buho-overline">{notice.date} · {notice.brand}</span><strong>{notice.title}</strong><small>{notice.status === "Pendiente" ? "Borrador listo para revisar" : "Comunicación gestionada"}</small></div><Tone tone={notice.urgency.toLowerCase()}>{notice.urgency}</Tone><i>→</i></button>)}</div><aside className="buho-notice-preview"><span className="buho-overline">CENTRO DE CORREOS</span><h2>Comunica sin alarmar.</h2><p>Cada alerta relevante incluye un borrador editable con el contexto y el próximo paso recomendado.</p><div><span>01</span>Revisa el contexto</div><div><span>02</span>Edita el borrador</div><div><span>03</span>Copia y envía</div></aside></section>;
}

function UsersView({ users, onAdd }: { users: DemoUser[]; onAdd: () => void }) { return <section className="buho-users"><header><div><span className="buho-overline">ORGANIZACIÓN</span><h2>Estudio Vial IP</h2></div><button className="buho-secondary" onClick={onAdd} type="button">Agregar usuario</button></header><div className="buho-user-list">{users.map((user) => <article key={user.id}><span>{user.initials}</span><div><strong>{user.name}</strong><small>{user.email}</small></div><div><small>FECHA DE CREACIÓN</small><strong>{user.createdAt}</strong></div><Tone tone="active">Activo</Tone><button type="button">···</button></article>)}</div><p className="buho-info-line"><span>i</span>En esta versión todos los usuarios tienen acceso completo a la organización.</p></section>; }

function SettingsView({ onToast, onReset, dataMode }: { onToast: (message: string) => void; onReset: () => void; dataMode: "loading" | "database" | "local" }) {
  const [toggles, setToggles] = useState([true, true, false, true]); const rows = [["Alertas de alta similitud", "Crear una notificación cuando aparezca una coincidencia alta."], ["Recordatorios de plazo", "Avisar 15, 7, 3 y 1 día antes del vencimiento."], ["Resumen semanal", "Preparar un resumen de actividad cada lunes."], ["Persistir filtros", "Recordar filtros y vistas utilizadas en cada módulo."]];
  return <section className="buho-settings"><aside><button className="is-active" type="button">Notificaciones</button><button type="button">Monitoreo</button><button type="button">Organización</button><button type="button">Plan y cupo</button></aside><div><span className="buho-overline">PREFERENCIAS · {dataMode === "database" ? "POSTGRESQL CONECTADO" : "ALMACENAMIENTO LOCAL"}</span><h2>Notificaciones y recordatorios</h2><p>{dataMode === "database" ? "Marcas, casos, usuarios y decisiones se guardan en la base compartida de Railway." : "Sin DATABASE_URL, la demo conserva los cambios solo en este navegador."}</p>{rows.map(([title, copy], index) => <label key={title}><div><strong>{title}</strong><small>{copy}</small></div><input aria-label={title} checked={toggles[index]} onChange={() => setToggles(toggles.map((value, itemIndex) => itemIndex === index ? !value : value))} type="checkbox" /><i /></label>)}<div className="buho-settings-actions"><button className="buho-primary" onClick={() => onToast("Preferencias guardadas")} type="button">Guardar cambios</button><button className="buho-danger" onClick={onReset} type="button">Restaurar datos demo</button></div></div></section>;
}

function DrawerShell({ title, eyebrow, onClose, children, wide = false }: { title: string; eyebrow: string; onClose: () => void; children: React.ReactNode; wide?: boolean }) { return <div className="buho-overlay" role="presentation" onMouseDown={(event) => { if (event.currentTarget === event.target) onClose(); }}><aside className={`buho-drawer ${wide ? "is-wide" : ""}`} role="dialog" aria-modal="true" aria-label={title}><header><div><span className="buho-overline">{eyebrow}</span><h2>{title}</h2></div><button aria-label="Cerrar" onClick={onClose} type="button">×</button></header>{children}</aside></div>; }

function BrandForm({ onClose, onSubmit, brandsCount }: { onClose: () => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void; brandsCount: number }) { return <DrawerShell eyebrow="NUEVA MARCA" onClose={onClose} title="Agregar al monitoreo"><form className="buho-form" onSubmit={onSubmit}><div className="buho-form-progress"><span className="is-current">1</span><i /><span>2</span><i /><span>3</span><small>Datos</small><small>Monitoreo</small><small>Confirmación</small></div><label>Nombre o representación denominativa<input name="name" placeholder="Ej. ALTURA" required /></label><label>Organización o titular<input name="owner" placeholder="Razón social" required /></label><div className="buho-form-grid"><label>Número de registro<input name="registration" placeholder="Opcional" /></label><label>País o jurisdicción<select defaultValue="Chile" name="country"><option>Chile</option><option>Argentina</option><option>Perú</option><option>Colombia</option></select></label></div><label>Clases de Niza<input name="classes" placeholder="Ej. 09, 35, 42" required /></label><label>Descripción de productos o servicios<textarea name="description" placeholder="Describe brevemente qué protege la marca" rows={4} /></label><div className="buho-upload"><span>ARCHIVOS ASOCIADOS</span><strong>Arrastra la representación gráfica o selecciónala</strong><small>PNG, JPG o PDF · máximo 10 MB</small><button type="button">Seleccionar archivo</button></div><p className="buho-capacity">Usarás el espacio {brandsCount + 1} de 25 incluido en el plan Estudio.</p><footer><button onClick={onClose} type="button">Cancelar</button><button className="buho-primary" type="submit">Agregar marca <span>→</span></button></footer></form></DrawerShell>; }

function CaseForm({ brands, users, onClose, onSubmit }: { brands: Brand[]; users: DemoUser[]; onClose: () => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) { return <DrawerShell eyebrow="NUEVO CASO" onClose={onClose} title="Crear caso"><form className="buho-form" onSubmit={onSubmit}><label>Título del caso<input name="title" placeholder="Ej. Oposición signo similar" required /></label><label>Marca relacionada<select name="brand">{brands.map((item) => <option key={item.id}>{item.name}</option>)}</select></label><label>Cliente<input name="client" placeholder="Organización propietaria" required /></label><div className="buho-form-grid"><label>Prioridad<select defaultValue="Media" name="priority"><option>Alta</option><option>Media</option><option>Baja</option></select></label><label>Próximo plazo<input name="deadline" placeholder="Ej. 30 ago" required /></label></div><label>Responsable<select name="owner">{users.map((user) => <option key={user.id}>{user.name}</option>)}</select></label><label>Descripción<textarea name="description" placeholder="Contexto y próximos pasos" rows={5} /></label><footer><button onClick={onClose} type="button">Cancelar</button><button className="buho-primary" type="submit">Crear caso <span>→</span></button></footer></form></DrawerShell>; }
function UserForm({ onClose, onSubmit }: { onClose: () => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) { return <DrawerShell eyebrow="ORGANIZACIÓN" onClose={onClose} title="Agregar usuario"><form className="buho-form" onSubmit={onSubmit}><label>Nombre completo<input name="name" placeholder="Nombre y apellido" required /></label><label>Correo electrónico<input name="email" placeholder="nombre@estudio.cl" required type="email" /></label><p className="buho-capacity">El usuario tendrá acceso completo a todos los módulos de esta demo.</p><footer><button onClick={onClose} type="button">Cancelar</button><button className="buho-primary" type="submit">Agregar usuario <span>→</span></button></footer></form></DrawerShell>; }

function MatchDrawer({ match, onClose, onReview }: { match: Match; onClose: () => void; onReview: (status: MatchStatus) => void }) { return <DrawerShell eyebrow={`${match.id} · ${match.source}`} onClose={onClose} title="Revisión de coincidencia" wide><div className="buho-drawer-scroll"><div className="buho-match-summary"><Tone tone={match.level.toLowerCase()}>{match.level}</Tone><strong>{match.score}%</strong><div><span>SIMILITUD TOTAL</span><small>Revisión pendiente · {match.date}</small></div><a href="https://www.inapi.cl/" target="_blank" rel="noreferrer">Abrir fuente oficial ↗</a></div><div className="buho-side-by-side"><article><span>MARCA MONITOREADA</span><div className="buho-mark-card">{match.brand}</div><dl><div><dt>ID</dt><dd>{match.brandId}</dd></div><div><dt>Titular</dt><dd>Cliente de la organización</dd></div><div><dt>Clases</dt><dd>29, 30, 35</dd></div></dl></article><i>↔</i><article><span>SIGNO ENCONTRADO</span><div className="buho-mark-card is-found">{match.found}</div><dl><div><dt>Solicitud</dt><dd>{match.application}</dd></div><div><dt>Solicitante</dt><dd>{match.applicant}</dd></div><div><dt>Publicación</dt><dd>{match.date}</dd></div></dl></article></div><section className="buho-score-breakdown"><header><div><span className="buho-overline">DESGLOSE DEL PUNTAJE</span><h3>Componentes de similitud</h3></div><small>Datos demostrativos</small></header>{[["Denominativa", Math.min(98, match.score + 3)], ["Fonética", Math.max(40, match.score - 2)], ["Visual", Math.max(35, match.score - 12)], ["Conceptual", Math.max(30, match.score - 18)], ["Clases o rubros", Math.max(55, match.score - 6)]].map(([label, value]) => <div className="buho-score-bar" key={String(label)}><span>{label}</span><i><b style={{ width: `${value}%` }} /></i><strong>{value}%</strong></div>)}</section><section className="buho-explanation"><span className="buho-overline">POR QUÉ SE DETECTÓ</span><p>El signo encontrado comparte la estructura denominativa principal, presenta una pronunciación muy próxima y coincide en clases relacionadas con la actividad de la marca monitoreada. Esta explicación corresponde a datos ficticios de la demo.</p></section><label className="buho-comment">Comentario interno<textarea placeholder="Agrega una observación para el equipo" rows={3} /></label></div><footer className="buho-drawer-actions"><button onClick={() => onReview("Descartada")} type="button">Descartar</button><button onClick={() => onReview("En observación")} type="button">Mantener en observación</button><button className="buho-primary" onClick={() => onReview("Convertida en caso")} type="button">Convertir en caso <span>→</span></button></footer></DrawerShell>; }

function CaseDrawer({ item, onClose, onMove }: { item: LegalCase; onClose: () => void; onMove: () => void }) { return <DrawerShell eyebrow={item.id} onClose={onClose} title={item.title}><div className="buho-drawer-scroll"><div className="buho-case-hero"><Tone tone={item.priority.toLowerCase()}>{item.priority}</Tone><Tone tone="active">{item.stage}</Tone><h3>{item.brand}</h3><p>{item.client}</p></div><section className="buho-detail-grid"><div><span>Responsable</span><strong>{item.owner}</strong></div><div><span>Próximo plazo</span><strong>{item.deadline}</strong></div><div><span>Coincidencia de origen</span><strong>{item.sourceMatch ?? "Creado manualmente"}</strong></div><div><span>Creado</span><strong>12 ago 2026</strong></div></section><section className="buho-case-section"><header><h3>Tareas</h3><button type="button">Agregar</button></header><label><input defaultChecked type="checkbox" />Revisar antecedentes de la publicación</label><label><input type="checkbox" />Confirmar instrucciones con cliente</label><label><input type="checkbox" />Preparar borrador de oposición</label></section><section className="buho-case-section"><header><h3>Actividad</h3></header><p><span>19 AGO</span>{item.owner} actualizó la prioridad del caso.</p><p><span>18 AGO</span>Se vinculó la comparación original.</p></section></div><footer className="buho-drawer-actions"><button onClick={onClose} type="button">Cerrar</button>{item.stage !== "Seguimiento" && <button className="buho-primary" onClick={onMove} type="button">Mover a siguiente etapa <span>→</span></button>}</footer></DrawerShell>; }

function NoticeDrawer({ notice, onClose, onChange, onCopy }: { notice: Notice; onClose: () => void; onChange: (patch: Partial<Notice>) => void; onCopy: (text: string, label: string) => void }) { return <DrawerShell eyebrow={`${notice.id} · ${notice.brand}`} onClose={onClose} title="Correo prehecho" wide><div className="buho-drawer-scroll"><div className="buho-mail-context"><Tone tone={notice.urgency.toLowerCase()}>{notice.urgency}</Tone><div><strong>{notice.title}</strong><small>Destinatario sugerido: contacto del cliente</small></div></div><label className="buho-mail-field">Asunto<div><input onChange={(event) => onChange({ subject: event.target.value })} value={notice.subject} /><button onClick={() => onCopy(notice.subject, "Asunto")} type="button">Copiar asunto</button></div></label><label className="buho-mail-field">Cuerpo del correo<textarea onChange={(event) => onChange({ body: event.target.value })} rows={16} value={notice.body} /><button onClick={() => onCopy(notice.body, "Correo")} type="button">Copiar correo</button></label><p className="buho-info-line"><span>i</span>Buho Marc no envía este correo. Puedes editarlo y copiarlo para enviarlo desde tu canal habitual.</p></div><footer className="buho-drawer-actions"><button onClick={onClose} type="button">Cerrar</button><button onClick={() => { onChange({ status: "Gestionada" }); onClose(); }} type="button">Marcar como enviado</button><button className="buho-primary" onClick={() => onCopy(notice.body, "Correo")} type="button">Copiar correo <span>→</span></button></footer></DrawerShell>; }
